import { FwooshPluginConfig } from "@fwoosh/types";
import { resolveFile } from "@fwoosh/types/resolve-file";
import { {{pascalCase name}}Options } from "./types.js";

export function {{camelCase name}}(options: {{pascalCase name}}Options) {
  return {
    tools: [
      {
        type: "",
        id: "{{kebabCase name}}",
        filepath: resolveFile("{{pascalCase name}}.js"),
        options,
      },
    ],
  } satisfies FwooshPluginConfig;
}