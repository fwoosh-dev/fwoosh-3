{
  "name": "fwoosh-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.9.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build"
  },
  "devDependencies": {
    "@turbo/gen": "^2.0.4",
    "turbo": "^2.0.4"
  }
}
